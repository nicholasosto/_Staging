import { FormGemDTO, RarityKey } from "shared";

export class GemFactory {
	/**
	 * Creates a new gem with the specified properties.
	 * @param {string} name - The name of the gem.
	 * @param {string} type - The type of the gem (e.g., "Ruby", "Sapphire").
	 * @param {number} power - The power level of the gem.
	 * @returns {Gem} The created gem object.
	 */
	public static createManifestationGem() {
		print("Creating a new manifestation gem...");
	}
}
