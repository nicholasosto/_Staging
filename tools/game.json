{
  "name": "game",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "rbxtsc --project tsconfig.json"
      },
      "outputs": ["out/**"]
    }
  }
}